{
  "name": "@quatecalc/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "refresh": "tsx src/refresh.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"(lint) worker\""
  },
  "dependencies": {
    "@quatecalc/contracts": "workspace:*",
    "@quatecalc/db": "workspace:*",
    "@quatecalc/scraper-adapters": "workspace:*",
    "@quatecalc/scraper-core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
